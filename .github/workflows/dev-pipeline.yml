name: DEV Pipeline

on:
  push:
    branches:
      - dev
  pull_request:
    branches:
      - dev

jobs:
  build-and-test:
    name: Build e Testes - DEV
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Código
        uses: actions/checkout@v4

      - name: Validação DEV
        run: |
          echo "🔍 Validando código no ambiente DEV..."
          echo "✅ Validação concluída!"

      - name: Deploy DEV (Simulado)
        run: |
          echo "📦 Deploy em ambiente DEV realizado!"
          echo "🌐 Ambiente disponível"
